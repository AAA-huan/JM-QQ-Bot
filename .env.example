# 环境配置文件示例
# 复制此文件并重命名为 .env 以使用自定义配置

# WebSocket服务端配置
# 下面修改host与port
NAPCAT_WS_URL=ws://host:port/qq

# API Token配置（可选）
# 用于NapCat WebSocket服务的身份验证
# 此Token需要与napcat_config.yml中的两个位置保持一致：
# 1. WebSocket服务配置部分的token字段
# 2. 默认中间件配置部分的access-token字段
# 留空表示不启用Token验证
NAPCAT_TOKEN=""

# 为兼容原配置保留此字段（与NAPCAT_TOKEN功能相同）
# 与napcat_config.yml中的access-token保持一致
ACCESS_TOKEN=''

# 漫画下载路径（相对仓库根目录）
MANGA_DOWNLOAD_PATH=./downloads
